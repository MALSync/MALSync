import { getInter } from '../ZeroScans/main';
import { pageInterface } from '../pageInterface';

const clone = getInter();

clone.name = 'HatigarmScanz';
clone.domain = 'https://hatigarmscanz.net';

export const HatigarmScanz: pageInterface = clone;
